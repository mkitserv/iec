---
interface Props {
  eyebrow?: string;
  title: string;
  intro?: string;
  align?: 'left' | 'center';
  dark?: boolean;
  num?: string;
}
const { eyebrow, title, intro, align = 'left', dark = false, num } = Astro.props;
const alignClass = align === 'center' ? 'text-center mx-auto items-center' : '';
---
<div class={`reveal flex flex-col max-w-3xl mb-12 md:mb-14 ${alignClass}`}>
  <div class={`flex items-baseline gap-4 ${align === 'center' ? 'justify-center' : ''}`}>
    {num && <span class="section-num">{num}</span>}
    {eyebrow && <p class={`eyebrow ${dark ? 'eyebrow-light' : ''}`}>{eyebrow}</p>}
  </div>
  <h2 class={`mt-4 text-3xl md:text-[2.7rem] leading-[1.06] tracking-tight ${dark ? 'text-white' : 'text-navy'}`}>{title}</h2>
  {intro && <p class={`mt-5 text-lg leading-relaxed ${dark ? 'text-white/65' : 'text-navy-light'}`}>{intro}</p>}
</div>
